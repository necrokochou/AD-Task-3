services:
  paruparo:
    build:
      context: .
    container_name: paruparo-service
    ports:
      - "8080:8000"
    develop:
      watch:
        - path: .
          target: /var/www/html
          action: sync
        - path: ./composer.json
          action: rebuild
        - path: ./composer.lock
          action: rebuild
    depends_on:
      - mongodb

  mongodb:
    image: mongo:latest
    container_name: paruparo-mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: rootPassword
      MONGO_INITDB_DATABASE: admin
    ports:
      - "27111:27017"
    volumes:
      - paruparo-mongodb:/data/db/mongodb

  postgresql:
    image: postgres:15
    container_name: paruparo-postgresql
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydatabase
    ports:
      - "5112:5432"
    volumes:
      - paruparo-postgresql:/data/db/postgresql

volumes:
  paruparo-mongodb:
    driver: local
  paruparo-postgresql:
    driver: local
